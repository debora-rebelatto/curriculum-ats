[build]
builder = "NIXPACKS"
buildCommand = "npm install --prefix frontend && npm run test && npm run build"

[deploy]
startCommand = "node server/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3